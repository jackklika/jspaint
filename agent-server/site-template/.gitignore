# Managed by the jspaint fork: agent-server/site-template/.gitignore — edit it there.

# renders of the page uploaded by JS Paint; regenerated on demand
screenshots/

node_modules/
dist/
.DS_Store

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
